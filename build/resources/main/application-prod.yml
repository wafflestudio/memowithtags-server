spring:
  datasource:
    url: jdbc:mysql://database-1.cn860meg8hj0.ap-northeast-2.rds.amazonaws.com:3306/database-1
    username: kiminho
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none # 운영 환경에서는 스키마 자동 변경을 피하기 위해 none 사용
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect # MySQL 8.x 버전 사용 시 적합
    show-sql: false # SQL 출력 비활성화 (운영 환경에서 로그 노출 방지)

server:
  port: 8080 # 필요에 따라 변경 가능
